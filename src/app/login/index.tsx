import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
} from 'react-native';
import auth from '@react-native-firebase/auth';

const Login = () => {
    return (
        <View>

        </View>
    )
}

export default Login;